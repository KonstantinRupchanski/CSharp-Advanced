using System;
using System.Collections.Generic;
using System.Linq;

namespace Problem01
{
    public class StartUp
    {
        public static void Main()
        {
            var bulletPrice = int.Parse(Console.ReadLine());
            var barrelSize = int.Parse(Console.ReadLine());
            var bullets = new List<int>(Console.ReadLine().Split().Select(int.Parse));
            var locks = new List<int>(Console.ReadLine().Split().Select(int.Parse));
            var intelligence = int.Parse(Console.ReadLine());
            bool outOfBullets = false;
            bool outOfLocks = false;







        }
    }
}
